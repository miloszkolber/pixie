{
	"name": "@pixie_ai/pixie-assistant",
	"private": true,
	"version": "0.1.0",
	"license": "Apache-2.0",
	"type": "module",
	"scripts": {
		"spike": "bun run src/pi-sdk-probe.ts",
		"spike:build": "bun build --compile --compile-autoload-package-json --target=bun --outfile pixie-pi-sdk-probe.bun-build src/pi-sdk-probe.ts",
		"test": "bun test tests",
		"typecheck": "tsc --noEmit && tsc --project src/tsconfig.json"
	},
	"devDependencies": {
		"@earendil-works/pi-coding-agent": "0.85.1",
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
