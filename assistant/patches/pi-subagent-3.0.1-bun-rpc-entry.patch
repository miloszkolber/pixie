diff --git a/runner.ts b/runner.ts
index b30500f0d68ec95739a7160bc782a035de0a63e8..cec8a55da4ad86073c3efd07a1a0801e8aa5c57d 100644
--- a/runner.ts
+++ b/runner.ts
@@ -9,6 +9,7 @@ import * as fs from "node:fs";
 import * as os from "node:os";
 import * as path from "node:path";
 import { StringDecoder } from "node:string_decoder";
+import { fileURLToPath } from "node:url";
 import type { AgentToolResult } from "@earendil-works/pi-agent-core";
 import {
   DEFAULT_MAX_BYTES,
@@ -75,6 +76,12 @@ export function getUnexpectedSignalFailure(
  * work on Unix and Windows without going through a shell wrapper.
  */
 function resolvePiSpawn(): { command: string; prefixArgs: string[] } {
+  if (/[\\/]bun(?:\.exe)?$/i.test(process.execPath)) {
+    return {
+      command: process.execPath,
+      prefixArgs: [fileURLToPath(import.meta.resolve("@earendil-works/pi-coding-agent/rpc-entry"))],
+    };
+  }
   const isNode = /[\\/]node(?:\.exe)?$/i.test(process.execPath);
   if (isNode && process.argv[1]) {
     return { command: process.execPath, prefixArgs: [process.argv[1]] };
