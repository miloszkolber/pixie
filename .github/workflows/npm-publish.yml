name: Publish pixie-assistant package

# Publishes @pixie_ai/pixie-assistant to the npm registry so the Pi service runs
# without a checkout: `bunx @pixie_ai/pixie-assistant@<version>`.
# Tags look like `pixie-assistant-v1.2.3`; the tag version stamps package.json at
# publish time and is never committed.

on:
  push:
    tags: ["pixie-assistant-v*"]
  workflow_dispatch:

concurrency:
  group: npm-publish-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    name: Source validation
    uses: ./.github/workflows/ci.yml
    with:
      revision: ${{ github.sha }}
    permissions:
      contents: read

  publish:
    name: Publish @pixie_ai/pixie-assistant
    needs: validate
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: ${{ github.sha }}

      - name: Derive package version
        id: source
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" && "$GITHUB_REF_NAME" == pixie-assistant-v* ]]; then
            echo "version=${GITHUB_REF_NAME#pixie-assistant-v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.0-dev" >> "$GITHUB_OUTPUT"
          fi

      - name: Guard release version
        id: guard
        env:
          PACKAGE_VERSION: ${{ steps.source.outputs.version }}
        run: |
          if [[ "$GITHUB_REF_TYPE" != "tag" ]]; then
            echo "Manual dispatch is validate-and-pack only; nothing will be published."
            echo "dry_run=true" >> "$GITHUB_OUTPUT"
          elif [[ ! "$PACKAGE_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "Refusing to publish non-semver version: $PACKAGE_VERSION" >&2
            exit 1
          fi

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version-file: package.json

      - name: Set up Node for publishing
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Install workspace dependencies
        run: bun install --frozen-lockfile

      - name: Stamp release version
        run: |
          bun -e 'const fs = require("node:fs"); const p = "assistant/package.json"; const manifest = JSON.parse(fs.readFileSync(p, "utf8")); manifest.version = process.env.PACKAGE_VERSION; fs.writeFileSync(p, JSON.stringify(manifest, null, "\t") + "\n");'
        env:
          PACKAGE_VERSION: ${{ steps.source.outputs.version }}

      - name: Verify packed contents
        working-directory: assistant
        env:
          PACKAGE_VERSION: ${{ steps.source.outputs.version }}
        run: |
          bun pm pack --dry-run 2>&1 | tee pack.log
          grep -q 'src/main.ts' pack.log
          grep -q 'scripts/apply-patches.mjs' pack.log
          grep -q 'patches/pi-subagent-3.0.1-bun-rpc-entry.patch' pack.log
          grep -q "pixie-assistant-${PACKAGE_VERSION}.tgz" pack.log
          if grep -q '0\.0\.0-dev' pack.log; then
            echo "Refusing to pack a 0.0.0-dev fallback version" >&2
            exit 1
          fi

      - name: Publish to npm over OIDC trusted publishing
        if: github.ref_type == 'tag'
        working-directory: assistant
        # No token: authentication comes from the workflow identity via
        # id-token:write. Requires npm >= 11.5.1 (Node 24 ships npm 11) and a
        # one-time trusted-publisher setup on the package (see deployment.md).
        run: npm publish --access public --provenance
